server {
    listen 80;
    server_name localhost;

    root /var/www/public;
    index index.php;

    # 🔹 Logs
    access_log /var/log/nginx/access.log;
    error_log /var/log/nginx/error.log;

    # 🔹 Empêche l'accès aux fichiers sensibles
    location ~ /\.(?!well-known).* {
        deny all;
    }

    # 🔹 Redirige tout vers index.php (Symfony routing)
    location / {
        try_files $uri /index.php$is_args$args;
    }

    # 🔹 Gestion de PHP via FastCGI (PHP-FPM)
    location ~ \.php$ {
        include fastcgi_params;
        fastcgi_pass php:9000; # correspond au service "php" du docker-compose
        fastcgi_index index.php;
        fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
        fastcgi_param DOCUMENT_ROOT $document_root;
        internal;
    }

    # 🔹 Augmente la taille max d'upload (pour images / vidéos)
    client_max_body_size 50M;

    # 🔹 Headers de sécurité (optionnel mais recommandé)
    add_header X-Frame-Options "SAMEORIGIN";
    add_header X-Content-Type-Options "nosniff";
    add_header X-XSS-Protection "1; mode=block";
}
